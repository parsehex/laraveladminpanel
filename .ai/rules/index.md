# Project Rules Index

Before planning or editing, find the row whose globs match the file's path and read that rule file.

| Applies to | Rule file |
| --- | --- |
| resources/views/{layouts,components}/** | .ai/rules/interface.md |
| tests/** | .ai/rules/tests.md |

## Project Context Info

This is a project that re-creates a past one, which is likely linked within the same workspace.

(In this & and these rule files, `me` refers to the user / the currently-sole developer.)

- `laraveladminpanel` is the new site
- `my-adminlte-site` is the old site
